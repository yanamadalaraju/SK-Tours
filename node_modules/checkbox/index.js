(function () {
    'use strict';

    angular.module('axialco.directives')
        .directive('axialcoCheckbox', directive);

    function directive() {
        return {
            template: '<span>checkbox</span>',
            bindToController: true,
            controllerAs: 'vm',
            controller: controller
        };

        function controller() {
            var vm = this;

            console.log('checkbox')
        };
    }
}());
